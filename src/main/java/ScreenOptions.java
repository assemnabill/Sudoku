public class ScreenOptions {
    final int X_OFFSET = 20;
    final int Y_OFFSET = 20;
    final int SIZE_TILE = 80;
    final int SIZE_BORDER = 10;
    int X_SIZE = 2 * X_OFFSET + SIZE_BORDER + 3 * (SIZE_TILE + SIZE_BORDER);
    int Y_SIZE = 2 * Y_OFFSET + SIZE_BORDER + 3 * (SIZE_TILE + SIZE_BORDER);
}
